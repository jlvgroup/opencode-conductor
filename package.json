{
  "name": "opencode-conductor",
  "version": "0.1.0",
  "description": "Self-owned minimal OpenCode V2 orchestration plugin: agent routing, background delegation, boulder continuation shim, push/merge approval guard.",
  "type": "module",
  "main": "src/plugin.js",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "node --test __tests__/"
  }
}
